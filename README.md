# Skills

This repository is used to organize and share custom agent skills.

## Install

Install a specific skill with the Skills CLI:

```bash
npx skills add vitorsoratto/skills --skill trident
```

List available skills in the repository:

```bash
npx skills add vitorsoratto/skills --list
```
