interface:
  display_name: "Trident"
  short_description: "Risk-triaged PR review with adaptive verification and concise verdicts"
  default_prompt: "Review the target with Trident. Start with deterministic risk triage, activate only required specialist capabilities, verify findings independently, and return a compact developer review plus a detailed remediation report when findings exist."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
