interface:
  display_name: "Trident"
  short_description: "Code review with quick and deep modes: scan, verify, and judge on evidence"
  default_prompt: "Review current git changes with Trident. Use quick mode for small local diffs and deep mode for PRs, ranges, directories, or risky code paths."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
