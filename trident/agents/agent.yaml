interface:
  display_name: "Trident"
  short_description: "Tool-aware code review with quick/deep specialist modules and evidence-based verdicts"
  default_prompt: "Review current git changes with Trident. Select the right tools and modules, use quick mode for small local diffs, and deep mode for PRs, ranges, directories, risky code paths, or edge-case-heavy logic."

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
